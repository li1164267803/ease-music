## 1. 选择器与界面

- [x] 1.1 `src/app/(tabs)/profile.tsx`：「选择目录 / 更换目录」改为 `chooseLyricsDirectory().finally(关面板)`（design 决策 1）。验证：见 5.1
- [x] 1.2 `src/ui/import-sheet.tsx`：播放列表面板的失败状态记下来源，只有地址导入失败才把地址输入框标红（决策 6）。验证：见 5.7
- [x] 1.3 `src/sources/local-file/index.ts` 导出 `keepsCopyOfPickedFiles`，`persistPickedFile` 与导入面板说明都读它（决策 7）。验证：见 5.8

## 2. 播放层

- [x] 2.1 `src/playback/player.ts`：抽出 `probe()`，`diagnose()` 改为基于它；`load()` 在交出音源后安排 `checkSlowLoad`，10 秒仍在加载时探测，服务端报错或声明为文本/网页即按失败处理（决策 2）。验证：见 5.3
- [x] 2.2 连续失败计数改为 `failedInStreak` 集合，跳过目标已失败即停，提示按实际曲目数描述（决策 3）。验证：见 5.4
- [x] 2.3 播放历史的记录从 `load()` 移到 `markSounding()`，以 `soundedToken` 保证每次装载只生效一次（决策 4）。验证：见 5.5

## 3. 歌词、缓存、播放列表解析

- [x] 3.1 `src/lyrics/sources.ts`：读取内嵌歌词时 `syncText` 兜底为空数组（USLT 没有这个字段）。验证：见 5.2
- [x] 3.2 `src/cache/download.ts`：下载看门狗，30 秒无新数据即 `task.cancel()` 并报告停滞（决策 5）。验证：见 5.6
- [x] 3.3 `src/library/m3u.ts`：含控制字符的内容整体判为零条目；非注释行只有带协议或以扩展名结尾的相对路径才算条目（决策 6）。`src/library/m3u.test.ts` 更新「不是播放列表的文本」用例，新增网页、二进制、扩展名与协议三组用例

## 4. 静态检查

- [x] 4.1 `pnpm lint`、`pnpm typecheck`、`pnpm test:m3u`（26 项）、`pnpm test:queue`（7 项）、`pnpm check:constraints`、`pnpm check:ios-strip` 全部通过

## 5. iOS 复验（iPhone 17 Pro 模拟器，iOS 26.3，2026-09-19）

- [x] 5.1 B1：「我的 → 歌词目录 → 选择目录」弹出目录选择器，选「歌词」文件夹后生效；随后导入播放列表的文件选择器照常弹出。补测被 B1 挡住的歌词目录场景：目录版 `优先级曲.lrc` 优先于内嵌歌词 ✓；`组合命名曲 - 组合艺人.lrc` 匹配 ✓；GBK 的 `编码曲.lrc` 提示「不是 UTF-8 编码，无法读取」且无乱码 ✓；重启应用后新放入目录的 `历史曲A.lrc` 仍能读到 ✓（模拟器沙盒较宽松，真机需复核）
- [x] 5.2 B3：`纯文本歌词曲`（只含 USLT）显示全部歌词行，无高亮
- [x] 5.3 B2：播放 `notaudio`（200 + `text/html`），第 10 秒探测一次，报「音频无法解码，格式不受支持或文件已损坏」并停下；该地址只装载一次
- [x] 5.4 B5：列表循环下单曲队列播放 `fail`（500），只请求一次，提示「「fail」播放失败：音频地址返回了错误（HTTP 500）」，不再出现「连续 5 首」。回归：歌单 plain / fail / slow 从 fail 开始，失败后自动跳到 slow 并发声，失败提示仍可见
- [x] 5.5 B4：清空历史后依次播放 5 首可播曲目与 `notaudio`、`fail`，数据库中只有 5 首可播曲目，各 1 次
- [x] 5.6 B6：下载 `stall` 到 4% 时停掉服务端，约 40 秒内转为失败状态；应用缓存目录与 `.partial` 无残留
- [x] 5.7 B7：播放列表选择器选中 `优先级曲.lrc` 与 `编码曲.mp3`，均提示「这个文件里没有可识别的条目，不是可用的播放列表。」，地址输入框不标红。回归：`本地列表.m3u` 仍为 3 首可导入、2 条跳过（相对路径、ftp）
- [x] 5.8 B8：iOS 导入面板说明为「可多选。应用内会另存一份，原文件不受影响。」

## 6. 待真机复核

- [ ] 6.1 锁屏与控制中心的媒体控件、封面显示；来电打断与其他应用抢占音频；耳机按键
- [ ] 6.2 真实断网（非服务端消失）下的下载停滞与播放失败提示
- [ ] 6.3 歌词目录的安全作用域访问在真机重启后仍有效
- [x] 6.4 Android 回归（Medium_Phone_API_36.0，2026-09-19）：
  - 播放失败：列表循环下单曲队列播放 `fail`，只装载一次（ExoPlayer 自带退避重试 4 个请求 + 1 次诊断探测）后停下，不再重复尝试；全部曲目队列里 `fail` 失败后自动跳到下一首「我记得」并发声，失败提示保留
  - 播放历史：`fail` 与 `notaudio` 不进历史；「我记得」发声后只加 1 次；暂停再继续不加
  - 装载探测：Android 上 `notaudio` 由 ExoPlayer 立即报错走原有路径，没有触发 10 秒探测；`slow` 正常发声，没有被误判
  - 下载：`slow`（40KB/s，约 48 秒）完整下载，看门狗没有误判；`stall` 下载中途停掉服务端，okhttp 立即报失败，`.partial` 无残留；恢复后重试下载完成
  - m3u：远程 `plain.mp3` 当播放列表导入提示「不是可用的播放列表」；`remote.m3u` 解析出 2 首
  - 歌词：纯文本（USLT）内嵌歌词正常显示（B3 在 Android 上同样存在，已一并修好）；同步歌词（SYLT）逐行高亮不受影响；歌词目录「更换目录」在面板打开时弹出 SAF 选择器，取消后面板关闭、原目录不变
  - 导入说明：Android 仍为「原文件留在原处，曲库只记录它的位置」
