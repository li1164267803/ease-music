## 1. 工程初始化

- [x] 1.1 使用 Expo SDK 57 创建工程，锁定 React Native 0.86，配置 TypeScript；`app.json` 按 design.md 决策 9 写死应用标识：`name` 为「自在音乐」、`slug` 为 `ease-music`、`android.package` 与 `ios.bundleIdentifier` 均为 `com.easemusic.app`
- [x] 1.2 配置 pnpm 作为包管理工具，建立目录结构与分层约定（领域层 / 来源层 / 播放层 / UI 层）
- [x] 1.3 配置代码规范（ESLint + Prettier）与路径别名
- [x] 1.4 安装 `expo-audio`（原定 `react-native-track-player`，改选理由见 design.md 决策 3），在 `app.json` 中配置 iOS `UIBackgroundModes: ["audio"]` 与 Android 前台服务权限
- [ ] 1.5 打通 Development Build 构建流程，在 iOS 与 Android 真机各跑通一次空应用
- [x] 1.6 建立开源仓库基础设施：`LICENSE`（GPL-3.0，已确定）、`CLA.md`、`.gitignore`、初始 README 骨架均已就位；本任务剩余部分为——在源文件模板中加入 GPL 头部声明块，并接入 CLA Assistant（合并任何外部 PR 之前必须完成）

## 2. 早期技术验证（阻塞后续实现）

- [x] 2.1 spike：验证 React Native 环境下的音频元数据解析方案。按 design.md 的优先级依次验证 `music-metadata` + polyfill、`jsmediatags`、自实现最小解析器，确定采用方案并记录结论
- [ ] 2.2 spike：验证 `expo-audio` 的媒体会话能力，确认锁屏控制、通知栏控制、耳机按键、音频焦点打断恢复在两端均可用；发现硬缺口立即上报
- [x] 2.3 将两个 spike 的结论回写到 design.md（如结论与原设计冲突，先更新设计再继续）

## 3. 领域模型与数据层

- [x] 3.1 定义曲目领域模型：曲目标识、来源标识、来源重解析所需信息（采用可扩展结构）、元数据字段
- [x] 3.2 定义统一的媒体解析契约类型（可播放地址 + 请求头 + User-Agent）
- [x] 3.3 接入 `expo-sqlite`，建立数据库连接与迁移机制（后续 change 会增改表结构）
- [x] 3.4 建立曲库表与歌单表结构，歌单关联表按 design.md 决策 8 建模（歌单 id、曲目 id、序号，含级联删除）
- [x] 3.5 实现曲库仓储层：曲目的增删查、去重（同来源同标识不重复入库）、排序与关键词检索
- [x] 3.6 实现歌单仓储层：歌单增删改、曲目加入/移除、歌单内排序、多对多查询
- [x] 3.7 接入轻量 KV 存储，实现播放模式等配置项的持久化

## 4. 媒体来源层

- [x] 4.1 实现来源注册表与按曲目来源标识路由解析的调度逻辑
- [x] 4.2 确保播放前的地址解析是唯一入口（为 C2 离线缓存预留拦截点）
- [x] 4.3 实现来源不存在时的降级处理（报告不可播放，不崩溃、不卡死队列）
- [x] 4.4 实现本地文件来源：文件选择器导入、持久化可访问的文件引用、解析为可播放地址
- [x] 4.5 处理本地文件失效场景（源文件被删除时标记曲目失效并向用户说明）
- [x] 4.6 实现远程 URL 来源：URL 合法性校验、入库、解析为可播放地址
- [x] 4.7 处理远程地址不可达场景（报告失败原因，曲目保留在曲库中）
- [x] 4.8 复查两个来源的实现路径，确认上层未依赖任何来源私有字段（抽象泄漏检查）

## 5. 播放引擎

- [x] 5.1 初始化播放服务，接入解析契约（播放时按来源实时解析地址，不缓存时效性地址）
- [x] 5.2 实现基本播放控制：播放、暂停、上一曲、下一曲、进度跳转
- [x] 5.3 实现播放队列：查看、追加、移除、跳转到指定曲目；处理移除当前曲目的场景
- [x] 5.4 实现四种播放模式（顺序、列表循环、单曲循环、随机）及其持久化
- [x] 5.5 配置系统媒体会话：锁屏与通知栏展示标题/艺术家/封面，响应系统媒体按键
- [x] 5.6 实现音频焦点与打断处理（来电暂停后恢复、被其他应用抢占时暂停）
- [x] 5.7 对外暴露播放状态（当前曲目、播放/暂停/缓冲/停止、进度、总时长）
- [x] 5.8 实现播放失败处理：向用户报告失败原因，单曲失败不中断整个队列

## 6. 曲库与歌单能力

- [x] 6.1 按 spike 2.1 的结论实现元数据解析（标题、艺术家、专辑、时长、音轨号）
- [x] 6.2 实现元数据缺失与解析失败时的降级（回退文件名，批量导入中单个失败不影响其余）
- [x] 6.3 实现内嵌封面提取与缓存，避免重复解析
- [x] 6.4 实现曲目导入流程：选择文件 → 解析元数据 → 提取封面 → 入库
- [x] 6.5 实现曲目移除（仅删除曲库记录，不删除设备上的原始文件，级联清理歌单条目）
- [x] 6.6 实现歌单管理：创建（拒绝空名称）、重命名、删除（不影响曲库中的曲目）
- [x] 6.7 实现曲目与歌单的关联操作：加入歌单（去重）、从歌单移除、歌单内拖动排序
- [x] 6.8 实现歌单播放：用歌单内容替换播放队列，支持从指定曲目开始；空歌单不清空当前队列

## 7. 界面

- [x] 7.1 搭建导航结构与基础主题
- [x] 7.2 实现曲库列表页：虚拟化长列表、排序切换、关键词检索、无封面时的占位
- [x] 7.3 实现导入入口：本地文件选择、远程 URL 添加表单（含无效输入提示）
- [x] 7.4 实现歌单列表页与歌单详情页：创建/重命名/删除、曲目拖动排序、加入与移除
- [x] 7.5 实现播放器页：封面、曲目信息、播放控制、进度条拖动、播放模式切换
- [x] 7.6 实现迷你播放器，在应用内跨页面常驻
- [x] 7.7 实现播放队列页：查看、跳转、移除
- [x] 7.8 实现播放状态与界面的同步（含从后台返回前台时的状态恢复）

## 8. 验证与收尾

- [ ] 8.1 在 iOS 与 Android 真机验证 media-playback 全部 spec 场景（重点：后台播放、锁屏控制、来电打断恢复）
- [ ] 8.2 验证 music-library 全部 spec 场景（重点：重启后曲库保留、元数据降级、数千曲目下的列表流畅度）
- [ ] 8.3 验证 playlist 全部 spec 场景（重点：一曲多歌单、歌单内排序持久化、曲库移除的级联清理）
- [ ] 8.4 验证 media-source 全部 spec 场景（重点：应用重启后本地文件仍可播放、源文件删除与远程地址失效的处理）
- [x] 8.5 确认核心能力不依赖任何插件模块，iOS 裁剪插件后仍为功能完整的播放器（design.md 决策 7 的持续约束）
- [x] 8.6 完善 README：项目定位、不提供音源的免责声明、开发环境搭建、构建说明、已锁定的技术栈版本；同时配置仓库的 GitHub topics（`music-player` `offline-music` `local-music` `android` `ios` `react-native` `expo`）与英文 description
- [x] 8.7 运行 `openspec validate --strict` 并解决问题
